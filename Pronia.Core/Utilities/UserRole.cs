namespace Pronia.Core.Utilities
{
    public static class UserRole
    {
        public enum Roles
        {
            Admin,
            Member
        }
        public const string Admin = "Admin";
        public const string Member = "Member"; 
    }
}
